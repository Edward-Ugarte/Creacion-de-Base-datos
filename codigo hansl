nulldata 100
setobs 1 1 --time-series

# Parámetros del modelo
scalar mu_x = 20
scalar sigma_x = 15
scalar alpha = 5
scalar beta = 2
scalar sigma_e = 10

# Generar x con media y sd exactas
series Z_ruido = normal()
series Z_estandarizada = (Z_ruido - mean(Z_ruido)) / sd(Z_ruido)
series x = mu_x + sigma_x * Z_estandarizada

# Generar el error ε
series e = normal()
series e_est = (e - mean(e)) / sd(e)
series u = sigma_e * e_est

# Generar y = α + β·x + ε
series y = alpha + beta * x + u

# Verificamos momentos y estimador
ols y const x
summary x y u
